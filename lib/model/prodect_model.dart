class ProdectModel {
  String? name, image, color, sized, price, descrabtion;
  ProdectModel({
    this.descrabtion,
    this.color,
    this.image,
    this.name,
    this.price,
    this.sized,
  });
}
